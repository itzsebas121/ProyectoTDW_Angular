import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'proyecto-angular',
  webDir: 'dist/proyecto-angular/browser',
};

export default config;
